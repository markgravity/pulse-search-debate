class SearchConfigs {
	static const searchingStatusInterval = Duration(seconds: 2);
	static const waitingForResponseTimeout = Duration(seconds: 15);
	static const countDownDuration = Duration(seconds: 3);
}